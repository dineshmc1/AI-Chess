# Server
PORT=8000
HOST=0.0.0.0
DEBUG=True

# Chess Engines
STOCKFISH_PATH=./stockfish-windows-x86-64-avx2/stockfish/stockfish-windows-x86-64-avx2.exe
LC0_PATH=./lc0/lc0.exe
MAIA_MODEL_PATH=./backend/models/maia-1900.pb.gz

# Database (Firebase)
FIREBASE_CREDENTIALS_PATH=./backend/firebase-adminsdk.json

# Redis (Celery Broker)
# If running locally without Docker/Redis, we might need a fallback or ensure Redis is installed.
# For now assuming local Redis or a cloud instance.
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# AI / LLM
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Optional: Model name if you want to make it configurable
LLM_MODEL_NAME=deepseek/deepseek-r1

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Lichess
LICHESS_API_URL=https://lichess.org/api
